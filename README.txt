to run navigate to this folders directory and run the command:

./chip8.exe "ROMS/romname.ch8" "cycle-rate"

you may need to experiment with the cycle rate so that the game
doesn't run too quickly, so try 3 or 5 to start out.

check controls by opening controls.png
it uses 1 to V like a numpad